services:
  postgres:
    image: 'postgres:17.5-alpine'
    environment:
      - POSTGRES_DB=nodlify
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=pass

    ports:
      - '5432:5432'
    networks:
      - nodlify

networks:
  nodlify:
    driver: bridge
