spring:

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        show_sql: true

  datasource:
    url: jdbc:postgresql://${NODLIFY_DATABASE_URL:localhost:5432/nodlify}
    username: ${NODLIFY_DATABASE_USER:root}
    password: ${NODLIFY_DATABASE_PASS:pass}
    name: nodlify
    hikari:
      maximum-pool-size: 20

  flyway:
    enabled: true
    baseline-on-migrate: true
    validate-on-migrate: true

# override observability config
nodlify:
  observability:
    problem:
      enabled: true
      logged-statuses: [ 400, 401, 403, 409, 500, 503 ]
    audit:
      enabled: true
